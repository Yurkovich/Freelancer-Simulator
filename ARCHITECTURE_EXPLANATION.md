# Объяснение архитектуры JavaScript-кода FreelancerSimulator

## 🎯 Что делает код в целом

Это браузерная игра-симулятор фрилансера, написанная на ванильном JavaScript с использованием ES6+ модулей. Игрок управляет персонажем, который развивает навыки, выполняет заказы, учится и зарабатывает деньги. Игра имеет систему сохранений, событий, жизненного цикла и сложную экономику с множеством взаимосвязанных систем.

---

## 🏗️ Архитектура проекта

### Принципы проектирования

**1. Модульная архитектура (ES6 Modules)**
- Каждый файл — отдельный модуль с четкой ответственностью
- Использование `import/export` для зависимостей
- Нет глобальных переменных (кроме `window.game` для отладки)

**2. ООП с применением SOLID принципов**
- **Single Responsibility**: каждый класс отвечает за одну область
  - `GameState` — управление состоянием
  - `TimeManager` — работа со временем
  - `OrderManager` — логика заказов
  - `WindowManager` — управление окнами
- **Dependency Injection**: менеджеры получают зависимости через конструктор
- **Composition over Inheritance**: используется композиция для связи компонентов

**3. Централизованное управление состоянием**
- `GameState` — единственный источник истины
- Все изменения состояния проходят через `updateState()`
- Автоматическое сохранение в `localStorage` при каждом изменении

---

## 📂 Структура проекта

### Точка входа: `main.js`

```javascript
// Инициализация игры при загрузке DOM
initGame() → создает Game → Game.init() → запускает игру
```

**Поток выполнения:**
1. `main.js` ждет загрузки DOM
2. Создает экземпляр `Game`
3. Привязывает обработчики событий к иконкам и кнопкам закрытия окон
4. `Game.init()` проверяет, пройден ли туториал, и показывает нужный экран

---

## 🔑 Ключевые компоненты

### 1. **Game (game.js)** — Главный оркестратор

**Ответственность:** Координирует работу всех менеджеров

```javascript
constructor() {
  this.gameState = new GameState()        // Состояние
  this.ui = new UIManager()               // UI
  this.audio = new AudioManager()         // Звук
  this.appsManager = new AppsManager()   // Приложения
  this.lifecycleManager = new LifecycleManager() // Жизненный цикл
  this.eventManager = new EventManager()  // События
}
```

**Почему так:**
- Централизованная точка доступа ко всем системам
- Упрощает тестирование и отладку
- Позволяет легко добавлять новые менеджеры

---

### 2. **GameState (state.js)** — Управление состоянием

**Ответственность:** Хранение, загрузка, сохранение и обновление игрового состояния

**Ключевые методы:**
- `loadState()` — загружает из `localStorage` или создает начальное состояние
- `updateState(newState)` — обновляет состояние и автоматически сохраняет
- `updateUI()` — синхронизирует DOM с состоянием
- `deepMerge()` — безопасное слияние объектов для миграций

**Особенности:**
- **Иммутабельность**: всегда возвращает копии состояния (`getState()`)
- **Автосохранение**: каждое изменение пишется в `localStorage`
- **Миграции**: `migrateSkills()`, `migrateOrders()` для совместимости со старыми сохранениями
- **Валидация**: проверяет корректность данных при загрузке

**Почему так:**
- Единый источник истины предотвращает рассинхронизацию
- Автосохранение улучшает UX
- Миграции позволяют обновлять структуру данных без потери прогресса

---

### 3. **AppsManager (apps.js)** — Менеджер приложений

**Ответственность:** Координирует работу всех игровых приложений

**Архитектура после рефакторинга:**
```
AppsManager
├── WindowManager      (управление окнами)
├── OrderManager       (логика заказов)
├── SleepManager       (логика сна)
├── BrowserManager     (браузер с заказами)
├── SkillsManager      (навыки)
├── LearningManager    (обучение)
└── ... другие менеджеры
```

**Паттерн:** Facade — упрощает доступ к сложной подсистеме

**Почему разделили:**
- **До рефакторинга**: 1019 строк, нарушение SRP
- **После**: 183 строки, каждый менеджер отвечает за свою область
- Легче тестировать и поддерживать

---

### 4. **TimeManager (time.js)** — Управление временем

**Ответственность:** Обработка игрового времени, переходы дней, сон

**Ключевая логика:**
```javascript
addTime(hours) {
  // 1. Вычисляет новое время
  // 2. Проверяет переход на новый день
  // 3. Триггерит жизненный цикл (сытость, доставки)
  // 4. При новом дне → onNewDay()
}
```

**Интеграция:**
- Работает с `LifecycleManager` для проверки сытости и доставок
- Уведомляет `AppsManager` о смене дня для обновления UI

**Почему так:**
- Время — центральный механизм игры
- Все действия влияют на время
- Централизация логики времени упрощает отладку

---

### 5. **OrderManager (order-manager.js)** — Логика заказов

**Ответственность:** Полный цикл работы с заказами

**Жизненный цикл заказа:**
1. `takeOrder()` — принятие заказа из браузера
2. `renderWZCode()` — отображение активного заказа
3. `workOnOrder()` — выполнение работы
4. `calculateWorkCost()` — расчет стоимости с учетом:
   - Уровня навыка игрока vs требуемого уровня
   - Улучшений (ПК, клавиатура, мышь)
   - Активных событий
5. `completeOrder()` — завершение и начисление наград

**Особенности:**
- Валидация заказов при загрузке (`syncActiveOrder()`)
- Поддержка "половинной работы" для высокоуровневых заказов
- Штрафы за просрочку дедлайна
- Бонусы за репутацию и улучшения

---

### 6. **LifecycleManager (lifecycle.js)** — Жизненный цикл

**Ответственность:** Ежедневные события, здоровье, больница

**Ключевые проверки:**
- `checkSatiety()` — уменьшение сытости каждые 3 часа
- `checkDeliveries()` — доставка купленных улучшений
- `onNewDay()` — оплата счетов, проверка просрочек
- `checkHospital()` — отправка в больницу при нулевом здоровье

**Почему отдельный менеджер:**
- Логика жизненного цикла сложная и независимая
- Легко тестировать отдельно
- Можно расширять новыми ежедневными событиями

---

### 7. **EventManager (event-manager.js)** — Система событий

**Ответственность:** Случайные события, модификаторы, эффекты

**Механика:**
1. `tryTriggerEvent()` — попытка триггера события
2. `rollEventType()` — случайный выбор типа (позитивное/негативное/двойное)
3. `selectRandomEvent()` — выбор конкретного события из доступных
4. `applyImmediateEffects()` — применение эффектов
5. `checkEventExpiration()` — проверка истечения события

**Типы событий:**
- **Позитивные**: бонусы к XP, деньгам, энергии
- **Негативные**: штрафы, блокировки
- **Двойные**: комбинация эффектов

**Почему так:**
- Добавляет непредсказуемость в игру
- События могут модифицировать любую систему через модификаторы
- Легко добавлять новые события в `events.js`

---

### 8. **UIManager (ui.js)** — Управление интерфейсом

**Ответственность:** Переключение экранов, тосты, звуки UI

**Ключевые функции:**
- `switchScreen()` — переключение между menu/dialog/desktop
- `showToast()` — показ уведомлений с автоскрытием
- `playSoundForMessage()` — звуковые эффекты для сообщений

**Почему так:**
- Централизация UI логики упрощает поддержку
- Единый стиль уведомлений
- Легко добавить анимации или эффекты

---

## 🔄 Потоки выполнения

### 1. Инициализация игры

```
main.js
  ↓
Game.constructor()
  ↓
Game.init()
  ↓
  ├─→ Если туториал пройден → startGame()
  └─→ Если нет → setupMenuScreen()
```

### 2. Выполнение действия (например, работа над заказом)

```
Пользователь кликает "Работать"
  ↓
OrderManager.workOnOrder()
  ↓
OrderManager.calculateWorkCost() (учитывает навыки, улучшения, события)
  ↓
OrderManager.processWork()
  ↓
TimeManager.addTime() → обновляет время
  ↓
LifecycleManager.checkSatiety() → проверяет сытость
  ↓
GameState.updateState() → сохраняет изменения
  ↓
GameState.updateUI() → обновляет DOM
```

### 3. Переход на новый день

```
TimeManager.addTime() обнаруживает переход дня
  ↓
TimeManager.onNewDay()
  ↓
LifecycleManager.onNewDay()
  ├─→ Проверка счетов
  ├─→ Доставка улучшений
  └─→ Обновление UI
  ↓
AppsManager.updateIconStates() → обновление блокировок
```

### 4. Обработка событий

```
Любое действие игрока
  ↓
EventManager.tryTriggerEvent() (случайная проверка)
  ↓
Если событие сработало:
  ├─→ Применение эффектов
  ├─→ Сохранение в state.activeEvent
  └─→ Отображение уведомления
```

---

## 💾 Обработка данных

### Состояние игры

**Структура:**
```javascript
{
  day, time, money, energy, health, satiety, reputation,
  skills: { layout: { level, xp }, ... },
  activeOrder: { ... },
  completedOrders: [...],
  upgrades: { pc: true, keyboard: true, ... },
  bills: { rent: {...}, internet: {...} },
  activeEvent: { id, effect, startDay },
  ...
}
```

**Принципы:**
- **Иммутабельность**: `getState()` всегда возвращает копию
- **Автосохранение**: каждое `updateState()` пишет в `localStorage`
- **Миграции**: при загрузке проверяется структура и мигрируется при необходимости

### Обработка событий

**DOM события:**
- Делегирование через `addEventListener` на иконках
- Обработка закрытия окон через `closest()`
- Предотвращение действий при блокировках

**Игровые события:**
- События триггерятся через `EventManager`
- Эффекты применяются через модификаторы
- Временные события автоматически истекают

---

## 🎨 Почему выбран такой подход

### 1. **Модульность и разделение ответственности**

**Проблема:** Большой монолитный файл сложно поддерживать

**Решение:** Разделение на менеджеры по областям ответственности

**Результат:**
- Легко найти нужный код
- Можно тестировать компоненты отдельно
- Проще добавлять новые функции

### 2. **Централизованное состояние**

**Проблема:** Рассинхронизация между разными частями UI

**Решение:** `GameState` как единственный источник истины

**Результат:**
- Нет дублирования данных
- Автоматическая синхронизация UI
- Предсказуемое поведение

### 3. **Dependency Injection**

**Проблема:** Жесткие зависимости усложняют тестирование

**Решение:** Передача зависимостей через конструктор

**Результат:**
- Легко мокировать для тестов
- Гибкая конфигурация
- Слабая связанность компонентов

### 4. **Композиция вместо наследования**

**Проблема:** Наследование создает жесткие связи

**Решение:** Менеджеры используют друг друга через композицию

**Результат:**
- Гибкая архитектура
- Легко переиспользовать компоненты
- Проще рефакторить

---

## 🎯 Важные детали для собеседования

### 1. **Архитектурные решения**

**"Я использовал паттерн Facade для AppsManager"**
- Скрывает сложность подсистемы приложений
- Упрощает API для остального кода
- Легко добавлять новые приложения

**"Применил принцип Single Responsibility"**
- После рефакторинга каждый класс отвечает за одну область
- `OrderManager` — только заказы
- `WindowManager` — только окна
- `SleepManager` — только сон

### 2. **Работа с состоянием**

**"Реализовал централизованное управление состоянием"**
- `GameState` как единственный источник истины
- Иммутабельные обновления через копирование
- Автоматическое сохранение в `localStorage`

**"Добавил систему миграций"**
- Позволяет обновлять структуру данных без потери прогресса
- `migrateSkills()`, `migrateOrders()` для обратной совместимости

### 3. **Обработка времени**

**"Время — центральный механизм игры"**
- Все действия влияют на время
- `TimeManager` координирует жизненный цикл
- Автоматические проверки при изменении времени

### 4. **Система событий**

**"Гибкая система случайных событий"**
- События могут модифицировать любую систему через модификаторы
- Легко добавлять новые события
- Поддержка временных эффектов

### 5. **Рефакторинг и улучшение кода**

**"Провел рефакторинг AppsManager"**
- Разделил 1019 строк на специализированные менеджеры
- Улучшил читаемость и поддерживаемость
- Соблюдение SOLID принципов

---

## 💬 Как объяснить устно (кратко и уверенно)

### 30-секундная версия:

*"Это браузерная игра-симулятор фрилансера на ванильном JavaScript. Архитектура модульная: каждый компонент — отдельный класс с четкой ответственностью. Состояние централизовано в GameState, который автоматически сохраняется в localStorage. Время управляется через TimeManager, который триггерит жизненный цикл. Я провел рефакторинг AppsManager, разделив его на специализированные менеджеры по принципу Single Responsibility. Использую композицию и dependency injection для слабой связанности компонентов."*

### 2-минутная версия:

*"Проект построен на модульной архитектуре с применением SOLID принципов. Точка входа — main.js, который создает Game класс. Game координирует менеджеры: GameState для состояния, TimeManager для времени, AppsManager для приложений, EventManager для событий.*

*GameState — единственный источник истины. Все изменения проходят через updateState(), который автоматически сохраняет в localStorage и обновляет UI. Использую иммутабельные обновления через копирование объектов.*

*После рефакторинга AppsManager разделен на WindowManager, OrderManager, SleepManager — каждый отвечает за свою область. Это улучшило читаемость и упростило тестирование.*

*TimeManager управляет игровым временем и триггерит жизненный цикл через LifecycleManager. EventManager добавляет случайные события, которые модифицируют игровые системы через систему модификаторов.*

*Все менеджеры получают зависимости через конструктор (dependency injection), что упрощает тестирование и делает код более гибким."*

---

## ⚠️ Слабые места и улучшения

### 1. **Обработка ошибок**

**Проблема:** Большинство методов не обернуты в try/catch

**Решение:**
```javascript
// Добавить обработку ошибок в критических местах
try {
  this.processWork(state, energyCost, timeRequired)
} catch (error) {
  console.error('Error processing work:', error)
  this.ui.showToast('Произошла ошибка при выполнении работы')
}
```

**Где применить:**
- `OrderManager.workOnOrder()`
- `TimeManager.addTime()`
- `GameState.updateState()`

### 2. **Валидация входных данных**

**Проблема:** Не все методы проверяют входные параметры

**Решение:**
```javascript
takeOrder(orderId) {
  if (!orderId || typeof orderId !== 'number') {
    console.warn('Invalid orderId:', orderId)
    return
  }
  // ...
}
```

### 3. **Типизация**

**Проблема:** Нет TypeScript или JSDoc комментариев

**Решение:**
- Добавить JSDoc для ключевых методов
- Или мигрировать на TypeScript

### 4. **Тестирование**

**Проблема:** Нет unit-тестов

**Решение:**
- Добавить Jest/Vitest
- Тестировать менеджеры изолированно
- Мокировать зависимости

### 5. **Производительность**

**Проблема:** Частые обновления DOM

**Решение:**
- Использовать `requestAnimationFrame` для батчинга обновлений
- Кешировать DOM элементы
- Использовать `DocumentFragment` для массовых операций

### 6. **Глобальные зависимости**

**Проблема:** Использование `window.game`, `window.audio`

**Решение:**
- Передавать зависимости явно через параметры
- Или использовать паттерн Service Locator

### 7. **Магические числа**

**Проблема:** Некоторые числа не вынесены в константы

**Решение:**
- Проверить все файлы на магические числа
- Вынести в `constants.js`

---

## 📊 Итоговая оценка архитектуры

### Сильные стороны:
✅ Модульная архитектура  
✅ Разделение ответственности  
✅ Централизованное состояние  
✅ Автосохранение  
✅ Система миграций  
✅ Гибкая система событий  
✅ Чистый код после рефакторинга  

### Области для улучшения:
⚠️ Обработка ошибок  
⚠️ Валидация входных данных  
⚠️ Тестирование  
⚠️ Типизация  
⚠️ Производительность DOM операций  

**Общая оценка: 8/10** — Хорошая архитектура с потенциалом для улучшения через добавление обработки ошибок и тестов.

---

## 🎓 Ключевые выводы для собеседования

1. **Архитектура:** Модульная, с применением SOLID принципов
2. **Состояние:** Централизованное, иммутабельное, с автосохранением
3. **Рефакторинг:** Улучшил код, разделив большой класс на специализированные
4. **Паттерны:** Facade, Dependency Injection, Composition
5. **Осознанность:** Понимаю слабые места и знаю, как их улучшить

